name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Fast JS checks. The generated @fpm/idl client is committed (not gitignored),
  # so lint/typecheck/test do NOT need the Anchor toolchain here.
  js:
    name: JS · lint · typecheck · test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # turbo --affected needs history
      - uses: pnpm/action-setup@v4 # reads packageManager from package.json
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: turbo affected (lint · typecheck · test)
        run: pnpm turbo run lint typecheck test --affected
        env:
          TURBO_SCM_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}

  # Anchor program build + confirm the committed client matches the IDL.
  anchor:
    name: Anchor · build · idl-fresh
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Solana CLI 3.1.10
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.10/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> "$GITHUB_PATH"
      - uses: dtolnay/rust-toolchain@stable # actual version pinned by rust-toolchain.toml
      - uses: Swatinem/rust-cache@v2
      # Install the anchor-cli directly from the pinned release tag. Building
      # `avm` from the anchor git HEAD broke (avm v1.1.2 fails to compile), so
      # we skip avm entirely and install the 1.0.2 CLI from its tag.
      - name: Install Anchor 1.0.2 (anchor-cli)
        run: cargo install --git https://github.com/solana-foundation/anchor --tag v1.0.2 anchor-cli --locked
      # --ignore-keys: CI has no program keypair (target/ is gitignored), so
      # anchor would flag a program-ID mismatch vs declare_id!. The emitted IDL
      # address comes from declare_id! (H59q, in source), not the keypair, so
      # the committed client still matches. We only build here, never deploy.
      - name: anchor build
        run: NO_DNA=1 anchor build --ignore-keys
      - name: cargo test (LiteSVM)
        run: cargo test --workspace
      # Regenerate the client and fail if the committed output drifted from the IDL.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm codegen
      - name: Verify @fpm/idl is up to date
        run: |
          git diff --exit-code libs/idl/src/generated \
            || (echo "::error::libs/idl/src/generated is stale — run 'anchor build && pnpm codegen' and commit the result" && exit 1)
