/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU128Decoder, getU128Encoder, getU16Decoder, getU16Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';

export const LEVERAGE_POOL_DISCRIMINATOR = new Uint8Array([226, 50, 217, 18, 167, 210, 119, 192]);

export function getLeveragePoolDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(LEVERAGE_POOL_DISCRIMINATOR); }

export type LeveragePool = { discriminator: ReadonlyUint8Array; 
/** The `Market` this pool writes options on; part of seeds. */
market: Address; 
/** Lev vault token account (`[b"lev_vault", market]`). */
vault: Address; 
/** LP share supply (internal accounting, no SPL mint). */
totalShares: bigint; 
/** Shares earmarked by `request_withdraw`, awaiting `withdraw_lp`. */
pendingWithdrawShares: bigint; 
/** Σ notional of open positions (bounded by `max_open_interest`). */
openInterest: bigint; 
/** Σ `max_gain` of open positions — pool liability bound (coverage input). */
totalMaxPayout: bigint; 
/** Last posted TxLINE marks (bps) [Team1, Draw, Team2]. */
markBps: Array<number>; 
/** Timestamp of the last `post_mark`; 0 until first post. */
markTs: bigint; 
/** Timestamp the funding index last accrued to. */
lastFundingTs: bigint; 
/** Cumulative funding index per outcome (INDEX_SCALE fixed point). */
cumFundingIndex: Array<bigint>; 
/** Opens rejected while `now < valve_paused_until`. */
valvePausedUntil: bigint; 
/** Funding multiplier (bps); BPS_DENOM = neutral, active in valve window. */
valveMultiplierBps: number; 
/** Multiplier applies while `now < valve_until_ts`. */
valveUntilTs: bigint; 
/** Canonical bump. */
bump: number; 
/** Canonical bump of the lev vault token account. */
vaultBump: number; 
/** Future. */
reserved: ReadonlyUint8Array;  };

export type LeveragePoolArgs = { 
/** The `Market` this pool writes options on; part of seeds. */
market: Address; 
/** Lev vault token account (`[b"lev_vault", market]`). */
vault: Address; 
/** LP share supply (internal accounting, no SPL mint). */
totalShares: number | bigint; 
/** Shares earmarked by `request_withdraw`, awaiting `withdraw_lp`. */
pendingWithdrawShares: number | bigint; 
/** Σ notional of open positions (bounded by `max_open_interest`). */
openInterest: number | bigint; 
/** Σ `max_gain` of open positions — pool liability bound (coverage input). */
totalMaxPayout: number | bigint; 
/** Last posted TxLINE marks (bps) [Team1, Draw, Team2]. */
markBps: Array<number>; 
/** Timestamp of the last `post_mark`; 0 until first post. */
markTs: number | bigint; 
/** Timestamp the funding index last accrued to. */
lastFundingTs: number | bigint; 
/** Cumulative funding index per outcome (INDEX_SCALE fixed point). */
cumFundingIndex: Array<number | bigint>; 
/** Opens rejected while `now < valve_paused_until`. */
valvePausedUntil: number | bigint; 
/** Funding multiplier (bps); BPS_DENOM = neutral, active in valve window. */
valveMultiplierBps: number; 
/** Multiplier applies while `now < valve_until_ts`. */
valveUntilTs: number | bigint; 
/** Canonical bump. */
bump: number; 
/** Canonical bump of the lev vault token account. */
vaultBump: number; 
/** Future. */
reserved: ReadonlyUint8Array;  };

/** Gets the encoder for {@link LeveragePoolArgs} account data. */
export function getLeveragePoolEncoder(): FixedSizeEncoder<LeveragePoolArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['market', getAddressEncoder()], ['vault', getAddressEncoder()], ['totalShares', getU64Encoder()], ['pendingWithdrawShares', getU64Encoder()], ['openInterest', getU64Encoder()], ['totalMaxPayout', getU64Encoder()], ['markBps', getArrayEncoder(getU16Encoder(), { size: 3 })], ['markTs', getI64Encoder()], ['lastFundingTs', getI64Encoder()], ['cumFundingIndex', getArrayEncoder(getU128Encoder(), { size: 3 })], ['valvePausedUntil', getI64Encoder()], ['valveMultiplierBps', getU16Encoder()], ['valveUntilTs', getI64Encoder()], ['bump', getU8Encoder()], ['vaultBump', getU8Encoder()], ['reserved', fixEncoderSize(getBytesEncoder(), 32)]]), (value) => ({ ...value, discriminator: LEVERAGE_POOL_DISCRIMINATOR }));
}

/** Gets the decoder for {@link LeveragePool} account data. */
export function getLeveragePoolDecoder(): FixedSizeDecoder<LeveragePool> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['market', getAddressDecoder()], ['vault', getAddressDecoder()], ['totalShares', getU64Decoder()], ['pendingWithdrawShares', getU64Decoder()], ['openInterest', getU64Decoder()], ['totalMaxPayout', getU64Decoder()], ['markBps', getArrayDecoder(getU16Decoder(), { size: 3 })], ['markTs', getI64Decoder()], ['lastFundingTs', getI64Decoder()], ['cumFundingIndex', getArrayDecoder(getU128Decoder(), { size: 3 })], ['valvePausedUntil', getI64Decoder()], ['valveMultiplierBps', getU16Decoder()], ['valveUntilTs', getI64Decoder()], ['bump', getU8Decoder()], ['vaultBump', getU8Decoder()], ['reserved', fixDecoderSize(getBytesDecoder(), 32)]]);
}

/** Gets the codec for {@link LeveragePool} account data. */
export function getLeveragePoolCodec(): FixedSizeCodec<LeveragePoolArgs, LeveragePool> {
    return combineCodec(getLeveragePoolEncoder(), getLeveragePoolDecoder());
}

export function decodeLeveragePool<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<LeveragePool, TAddress>;
export function decodeLeveragePool<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<LeveragePool, TAddress>;
export function decodeLeveragePool<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<LeveragePool, TAddress> | MaybeAccount<LeveragePool, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLeveragePoolDecoder());
}

export async function fetchLeveragePool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LeveragePool, TAddress>> {
  const maybeAccount = await fetchMaybeLeveragePool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLeveragePool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LeveragePool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLeveragePool(maybeAccount);
}

export async function fetchAllLeveragePool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LeveragePool>[]> {
  const maybeAccounts = await fetchAllMaybeLeveragePool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLeveragePool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LeveragePool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLeveragePool(maybeAccount));
}

export function getLeveragePoolSize(): number {
  return 226;
}