/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU128Decoder, getU128Encoder, getU16Decoder, getU16Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';

export const LEV_POSITION_DISCRIMINATOR = new Uint8Array([192, 105, 7, 71, 142, 158, 187, 18]);

export function getLevPositionDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(LEV_POSITION_DISCRIMINATOR); }

export type LevPosition = { discriminator: ReadonlyUint8Array; 
/** Binds; part of seeds. */
market: Address; 
/** Binds; part of seeds. */
owner: Address; 
/** Outcome index: 0 = Team1, 1 = Draw, 2 = Team2. */
outcomeIdx: number; 
/** Leverage multiple L (whole ×). */
leverage: number; 
/** Collateral C deposited into the lev vault at open. */
collateral: bigint; 
/** Notional N = C·L. */
notional: bigint; 
/** $1-payout-equivalent units U = floor(N·BPS / p_entry). */
units: bigint; 
/** Posted mark (bps) at open. */
entryMarkBps: number; 
/** `cum_funding_index[outcome_idx]` snapshot at open. */
fundingIndexSnap: bigint; 
/** Open timestamp. */
openTs: bigint; 
/** Settle guard (close/expire flips it; account then closed). */
settled: boolean; 
/** Canonical bump. */
bump: number; 
/** Future. */
reserved: ReadonlyUint8Array;  };

export type LevPositionArgs = { 
/** Binds; part of seeds. */
market: Address; 
/** Binds; part of seeds. */
owner: Address; 
/** Outcome index: 0 = Team1, 1 = Draw, 2 = Team2. */
outcomeIdx: number; 
/** Leverage multiple L (whole ×). */
leverage: number; 
/** Collateral C deposited into the lev vault at open. */
collateral: number | bigint; 
/** Notional N = C·L. */
notional: number | bigint; 
/** $1-payout-equivalent units U = floor(N·BPS / p_entry). */
units: number | bigint; 
/** Posted mark (bps) at open. */
entryMarkBps: number; 
/** `cum_funding_index[outcome_idx]` snapshot at open. */
fundingIndexSnap: number | bigint; 
/** Open timestamp. */
openTs: number | bigint; 
/** Settle guard (close/expire flips it; account then closed). */
settled: boolean; 
/** Canonical bump. */
bump: number; 
/** Future. */
reserved: ReadonlyUint8Array;  };

/** Gets the encoder for {@link LevPositionArgs} account data. */
export function getLevPositionEncoder(): FixedSizeEncoder<LevPositionArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['market', getAddressEncoder()], ['owner', getAddressEncoder()], ['outcomeIdx', getU8Encoder()], ['leverage', getU16Encoder()], ['collateral', getU64Encoder()], ['notional', getU64Encoder()], ['units', getU64Encoder()], ['entryMarkBps', getU16Encoder()], ['fundingIndexSnap', getU128Encoder()], ['openTs', getI64Encoder()], ['settled', getBooleanEncoder()], ['bump', getU8Encoder()], ['reserved', fixEncoderSize(getBytesEncoder(), 16)]]), (value) => ({ ...value, discriminator: LEV_POSITION_DISCRIMINATOR }));
}

/** Gets the decoder for {@link LevPosition} account data. */
export function getLevPositionDecoder(): FixedSizeDecoder<LevPosition> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['market', getAddressDecoder()], ['owner', getAddressDecoder()], ['outcomeIdx', getU8Decoder()], ['leverage', getU16Decoder()], ['collateral', getU64Decoder()], ['notional', getU64Decoder()], ['units', getU64Decoder()], ['entryMarkBps', getU16Decoder()], ['fundingIndexSnap', getU128Decoder()], ['openTs', getI64Decoder()], ['settled', getBooleanDecoder()], ['bump', getU8Decoder()], ['reserved', fixDecoderSize(getBytesDecoder(), 16)]]);
}

/** Gets the codec for {@link LevPosition} account data. */
export function getLevPositionCodec(): FixedSizeCodec<LevPositionArgs, LevPosition> {
    return combineCodec(getLevPositionEncoder(), getLevPositionDecoder());
}

export function decodeLevPosition<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<LevPosition, TAddress>;
export function decodeLevPosition<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<LevPosition, TAddress>;
export function decodeLevPosition<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<LevPosition, TAddress> | MaybeAccount<LevPosition, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLevPositionDecoder());
}

export async function fetchLevPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LevPosition, TAddress>> {
  const maybeAccount = await fetchMaybeLevPosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLevPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LevPosition, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLevPosition(maybeAccount);
}

export async function fetchAllLevPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LevPosition>[]> {
  const maybeAccounts = await fetchAllMaybeLevPosition(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLevPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LevPosition>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLevPosition(maybeAccount));
}

export function getLevPositionSize(): number {
  return 143;
}