/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type EncodedAccount, type FetchAccountConfig, type FetchAccountsConfig, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';

export const LP_ACCOUNT_DISCRIMINATOR = new Uint8Array([23, 166, 251, 222, 23, 27, 215, 48]);

export function getLpAccountDiscriminatorBytes() { return fixEncoderSize(getBytesEncoder(), 8).encode(LP_ACCOUNT_DISCRIMINATOR); }

export type LpAccount = { discriminator: ReadonlyUint8Array; 
/** Binds; part of seeds. */
market: Address; 
/** Binds; part of seeds. */
owner: Address; 
/** Free shares (excludes pending). */
shares: bigint; 
/** Shares earmarked by `request_withdraw`. */
pendingShares: bigint; 
/** Pending shares claimable after this timestamp. */
unlockTs: bigint; 
/** Canonical bump. */
bump: number; 
/** Future. */
reserved: ReadonlyUint8Array;  };

export type LpAccountArgs = { 
/** Binds; part of seeds. */
market: Address; 
/** Binds; part of seeds. */
owner: Address; 
/** Free shares (excludes pending). */
shares: number | bigint; 
/** Shares earmarked by `request_withdraw`. */
pendingShares: number | bigint; 
/** Pending shares claimable after this timestamp. */
unlockTs: number | bigint; 
/** Canonical bump. */
bump: number; 
/** Future. */
reserved: ReadonlyUint8Array;  };

/** Gets the encoder for {@link LpAccountArgs} account data. */
export function getLpAccountEncoder(): FixedSizeEncoder<LpAccountArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['market', getAddressEncoder()], ['owner', getAddressEncoder()], ['shares', getU64Encoder()], ['pendingShares', getU64Encoder()], ['unlockTs', getI64Encoder()], ['bump', getU8Encoder()], ['reserved', fixEncoderSize(getBytesEncoder(), 16)]]), (value) => ({ ...value, discriminator: LP_ACCOUNT_DISCRIMINATOR }));
}

/** Gets the decoder for {@link LpAccount} account data. */
export function getLpAccountDecoder(): FixedSizeDecoder<LpAccount> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['market', getAddressDecoder()], ['owner', getAddressDecoder()], ['shares', getU64Decoder()], ['pendingShares', getU64Decoder()], ['unlockTs', getI64Decoder()], ['bump', getU8Decoder()], ['reserved', fixDecoderSize(getBytesDecoder(), 16)]]);
}

/** Gets the codec for {@link LpAccount} account data. */
export function getLpAccountCodec(): FixedSizeCodec<LpAccountArgs, LpAccount> {
    return combineCodec(getLpAccountEncoder(), getLpAccountDecoder());
}

export function decodeLpAccount<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<LpAccount, TAddress>;
export function decodeLpAccount<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<LpAccount, TAddress>;
export function decodeLpAccount<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<LpAccount, TAddress> | MaybeAccount<LpAccount, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getLpAccountDecoder());
}

export async function fetchLpAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LpAccount, TAddress>> {
  const maybeAccount = await fetchMaybeLpAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLpAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LpAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLpAccount(maybeAccount);
}

export async function fetchAllLpAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LpAccount>[]> {
  const maybeAccounts = await fetchAllMaybeLpAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLpAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LpAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLpAccount(maybeAccount));
}

export function getLpAccountSize(): number {
  return 113;
}